<Window x:Class="Annuaire.Views.AddContact"
        xmlns:ViewModels="clr-namespace:Annuaire.ViewModels"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
        xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
		xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
        mc:Ignorable="d"
        Title="Modification:PRISMASOFT" Height="600" Width="1300" 
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Company Information Fields -->
        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Nom" VerticalAlignment="Center"/>
            <dxe:TextEdit Text="{Binding Nom}" Grid.Row="0"  Grid.Column="1" Margin="5" />

            <TextBlock  Margin="10,0,0,0" Grid.Row="0" Grid.Column="2" Text="Standard téléphonique" VerticalAlignment="Center"/>
            <dxe:TextEdit Text="{Binding Standard}" Grid.Row="0" Grid.Column="3" Margin="5" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Adresse" VerticalAlignment="Center"/>
            <dxe:TextEdit Text="{Binding Adresse}" Grid.Row="1" Grid.Column="1" Margin="5" />

            <TextBlock  Margin="10,0,0,0" Grid.Row="1" Grid.Column="2" Text="Adresse2" VerticalAlignment="Center"/>
            <dxe:TextEdit Text="{Binding Adresse2}" Grid.Row="1" Grid.Column="3" Margin="5" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Code Postal" VerticalAlignment="Center"/>
            <dxe:TextEdit Text="{Binding CodePostal}" Grid.Row="2" Grid.Column="1" Margin="5" />

            <TextBlock  Margin="10,0,0,0" Grid.Row="2" Grid.Column="2" Text="Ville" VerticalAlignment="Center"/>
            <dxe:TextEdit Text="{Binding Ville}" Grid.Row="2" Grid.Column="3" Margin="5" />
        </Grid>

        <!-- Contacts Section -->
        <GroupBox Grid.Row="3" Header="Contacts" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Contacts Grid -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

					<dxe:ButtonEdit Grid.Row="0" NullText="Entrez le texte à rechercher...">
						<dxe:ButtonEdit.Buttons>
							<dxe:ButtonInfo GlyphKind="Search"/>
						</dxe:ButtonEdit.Buttons>
					</dxe:ButtonEdit>

					<dxg:GridControl Grid.Row="1" Margin="0,5,0,0"
                            ItemsSource="{Binding Contacts}"
                            SelectedItem="{Binding SelectedContact}">
                        <dxg:GridControl.Columns>
							<dxg:GridColumn FieldName="Civilite" Width="10">
								<dxg:GridColumn.EditSettings>
									<dxe:ComboBoxEditSettings
										IsTextEditable="True"
										ItemsSource="{Binding DataContext.CiviliteItems, RelativeSource={RelativeSource AncestorType=Window}}"
										DisplayMember="Text"
										ValueMember="Value"/>
								</dxg:GridColumn.EditSettings>
							</dxg:GridColumn>
                            <dxg:GridColumn FieldName="Nom" Width="20"/>
                            <dxg:GridColumn FieldName="Prenom" Width="20"/>
                            <dxg:GridColumn FieldName="Fonction" Width="20"/>
                        </dxg:GridControl.Columns>
                        <dxg:GridControl.View>
                            <dxg:TableView 
                                ShowGroupPanel="False" 
                                NewItemRowPosition="Top"
                                AutoWidth="True"/>
                        </dxg:GridControl.View>
                    </dxg:GridControl>
                </Grid>

                <!-- Infos Contact -->
                <Grid Grid.Column="1" Margin="10,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Infos contact" FontWeight="Bold"/>

					<dxg:GridControl Grid.Row="1"
                 Margin="0,5,0,0"
                 ItemsSource="{Binding ContactInfos}"
                 SelectedItem="{Binding SelectedContactInfo, Mode=TwoWay}">
						<dxg:GridControl.Columns>
							<dxg:GridColumn FieldName="TypeInfo" Width="50">
								<dxg:GridColumn.EditSettings>
									<dxe:ComboBoxEditSettings
										IsTextEditable="True"
										ItemsSource="{Binding DataContext.TypeInfoItems, RelativeSource={RelativeSource AncestorType=Window}}"
										DisplayMember="Text"
										ValueMember="Value"/>
								</dxg:GridColumn.EditSettings>
							</dxg:GridColumn>
							<dxg:GridColumn FieldName="Info" Width="50"/>
							<dxg:GridColumn Width="30" FixedWidth="True" AllowEditing="False">
								<dxg:GridColumn.CellTemplate>
									<DataTemplate>
										<Button Command="{Binding DataContext.DeleteContactInfoCommand, RelativeSource={RelativeSource AncestorType=Window}}"
												CommandParameter="{Binding}">
											<Button.Content>
												<TextBlock Text="✕"
														  FontFamily="Segoe UI Symbol"
														  HorizontalAlignment="Center"
														  VerticalAlignment="Center"/>
											</Button.Content>
										</Button>
									</DataTemplate>
								</dxg:GridColumn.CellTemplate>
							</dxg:GridColumn>
						</dxg:GridControl.Columns>
						<dxg:GridControl.View>
							<dxg:TableView
								ShowGroupPanel="False"
								NewItemRowPosition="Top"
								AutoWidth="True"/>
						</dxg:GridControl.View>
					</dxg:GridControl>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Button Panel -->
        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" 
					Content="Valider" 
					Command="{Binding SaveCommand}" 
					Width="120" 
					Margin="0,0,5,0"/>
            <Button Grid.Column="2" 
					Content="Annuler" 
					Command="{Binding CancelCommand}" 
					Width="120"/>
        </Grid>
    </Grid>
</Window>